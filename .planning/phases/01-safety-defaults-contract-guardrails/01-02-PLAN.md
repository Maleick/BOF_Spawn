---
phase: 01-safety-defaults-contract-guardrails
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - BOF_spawn.cna
  - Src/Bof.c
autonomous: true
requirements: [SAFE-02]
must_haves:
  truths:
    - Callback execution is blocked when CFG-disable is off.
    - Risk/precondition warnings are shown before execution only.
    - Failure output is one-line cause plus next step; diagnostics escalate only on explicit debug mode.
  artifacts:
    - BOF_spawn.cna
    - Src/Bof.c
  key_links:
    - BOF_spawn.cna preflight validation blocks callback+CFG mismatch before bof_pack.
    - Src/Bof.c mirrors callback+CFG invariant as runtime fail-closed check.
---

<objective>
Add explicit callback/CFG guardrails and operator feedback contract enforcement.

Purpose: fail closed on invalid callback conditions with clear remediation.
Output: preflight hard block in CNA and defensive invariant in BOF.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-safety-defaults-contract-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-defaults-contract-guardrails/01-RESEARCH.md
@BOF_spawn.cna
@Src/Bof.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared CNA preflight gate for callback CFG dependency</name>
  <files>BOF_spawn.cna</files>
  <action>Create a common validation helper used by both aliases. If callback mode is selected and CFG-disable is off, hard-block execution and emit clear remediation to enable CFG-disable then retry.</action>
  <verify><automated>rg -n 'callback.*blocked|enable CFG-disable|preflight|spawn_beacon|spawn_shellcode' BOF_spawn.cna</automated></verify>
  <done>Neither alias allows callback dispatch without CFG-disable enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce action-first concise warning and debug escalation style</name>
  <files>BOF_spawn.cna</files>
  <action>Add explicit debug toggle plumbing so default mode stays concise and fix-oriented. Ensure warnings are pre-execution only and default failures are one-line cause plus next step.</action>
  <verify><automated>rg -n 'debug|warning|next step|callback' BOF_spawn.cna</automated></verify>
  <done>Default output is low-noise with optional deeper diagnostics only when requested.</done>
</task>

<task type="auto">
  <name>Task 3: Add BOF-side callback precondition defense</name>
  <files>Src/Bof.c</files>
  <action>Add early check so callback execution path fails closed when CFG-disable is off, even if CNA-side validation is bypassed. Return concise remediation-oriented error text.</action>
  <verify><automated>rg -n 'HIJACK_RIP_CALLBACK|DisableCfg|requires CFG|BeaconPrintf\(CALLBACK_ERROR' Src/Bof.c</automated></verify>
  <done>Runtime callback flow never proceeds with invalid CFG state.</done>
</task>

</tasks>

<verification>
- SAFE-02 mapped and complete.
- Hard block + remediation verified in CNA.
- BOF invariant check present as backup safety net.
</verification>

<success_criteria>
- [ ] SAFE-02 fully addressed.
- [ ] Callback+CFG mismatch cannot execute.
- [ ] Operator feedback style contract enforced.
</success_criteria>

<output>
After completion, create .planning/phases/01-safety-defaults-contract-guardrails/01-02-SUMMARY.md.
</output>
