---
phase: 01-safety-defaults-contract-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BOF_spawn.cna
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - Default profile uses RW->RX behavior unless operator explicitly opts into RWX.
    - Default execution method is callback-disabled by default posture.
    - Default output shows concise action-first safety posture before execution.
  artifacts:
    - BOF_spawn.cna
  key_links:
    - BOF_spawn.cna default variables feed ProcessConfig and both alias command paths.
---

<objective>
Audit and align CNA defaults/messages with strict-safe execution posture.

Purpose: eliminate unsafe ambiguity at session start.
Output: single, explicit strict-safe default profile applied consistently.
</objective>

<execution_context>
@/Users/maleick/.codex/get-shit-done/workflows/execute-plan.md
@/Users/maleick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-safety-defaults-contract-guardrails/01-CONTEXT.md
@.planning/phases/01-safety-defaults-contract-guardrails/01-RESEARCH.md
@BOF_spawn.cna
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace unsafe defaults with strict-safe baseline</name>
  <files>BOF_spawn.cna</files>
  <action>Set default profile so RWX stays opt-in and callback is not the default execution path. Preserve session-persistent behavior by keeping defaults as script-level state variables initialized once at load.</action>
  <verify><automated>rg -n '^\$use_rwx = "false";|^\$exec = "Hijack RIP Direct";' BOF_spawn.cna</automated></verify>
  <done>Fresh script session starts with non-RWX and non-callback default posture.</done>
</task>

<task type="auto">
  <name>Task 2: Align dialog text to strict-safe profile</name>
  <files>BOF_spawn.cna</files>
  <action>Update dialog description and configuration summary output to explicitly state strict-safe defaults and RWX manual opt-in semantics while keeping language concise and action-first.</action>
  <verify><automated>rg -n 'strict-safe|manual opt-in|Use RWX|Execution method|Configuration Update' BOF_spawn.cna</automated></verify>
  <done>Dialog/help text and apply output accurately reflect the intended safety baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize startup output for both aliases</name>
  <files>BOF_spawn.cna</files>
  <action>Ensure `spawn_beacon` and `spawn_shellcode` display the same concise policy snapshot for Block DLL, CFG, RWX, and execution mode before dispatching BOF execution.</action>
  <verify><automated>rg -n 'spawn_beacon|spawn_shellcode|Block DLL:|Disable CFG:|Use RWX:|Execution:' BOF_spawn.cna</automated></verify>
  <done>Both aliases report deterministic, low-noise policy output.</done>
</task>

</tasks>

<verification>
- SAFE-01 appears in plan requirements and task outcomes.
- Defaults are strict-safe and non-ambiguous.
- Alias output remains concise and consistent.
</verification>

<success_criteria>
- [ ] SAFE-01 fully addressed.
- [ ] RWX remains opt-in only.
- [ ] Callback is not default execution mode.
</success_criteria>

<output>
After completion, create .planning/phases/01-safety-defaults-contract-guardrails/01-01-SUMMARY.md.
</output>
